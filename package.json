{
  "name": "wagmi-sig",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ dawksh/wagmi-sig",
    "create-local": "graph create --node http://localhost:8020/ dawksh/wagmi-sig",
    "remove-local": "graph remove --node http://localhost:8020/ dawksh/wagmi-sig",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dawksh/wagmi-sig"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.1",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
